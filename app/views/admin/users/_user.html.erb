<!-- user - такой итератор который команда render @users сама отправит в этот паршал ( автопилот :) ) -->
<tr>
    <th scope="col"><%= user.id %></th>
    <td><%= user.name %></td>
    <td><%= user.email %></td>
    <td><%= user.role %></td>
    <td><%= user.created_at %></td>
    <td>
        <%= link_to t('.edit_user'), edit_admin_user_path(user), class: 'btn btn-primary'%>
        <%= link_to t('.delete_user'), admin_user_path(user), class: 'btn btn-danger', data: {method: :delete, confirm: t('.confirm')}%>
    </td>
</tr>