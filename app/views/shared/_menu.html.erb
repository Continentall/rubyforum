<%= provide :main_menu do %> 
    <header class="p-2 bg-light text-black shadow-sm ">
        <div class="container">
            <nav class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
            <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
                <li><%= nav_tab 'Главная страница',root_path, class: 'nav-link px-2', current_page: local_current_page %></li> <!-- root - префикс страницы (смотреть в rails/routes), куда мы переходим, _path - указание на отностительность ссылки --->
                <li><%= nav_tab 'Обсуждения',topics_path, class: 'nav-link px-2', current_page: local_current_page %></li> <!-- Почему-то нужно указывать класс после пути! Иначе ошибка( -->
            </ul>

            <ul class="nav col-12 col-lg-auto mb-2 mb-md-0">
                <% if user_signed_in? %> <!-- ПРОЗРЕНИЕ user_signed_in? (внутри эта функция вызывает current_user ) делает нам вход в аккаунт через куки или сессию каждый-каждый раз при запуске или переходе с страницы на странцу -->
                    <li class="dropdown">
                        <%= nav_tab current_user.name_or_email ,'#', class: 'nav-link px-2 text-black dropdown-toggle', data: {"bs-toggle": 'dropdown'}, current_page: local_current_page %>
                        <ul class="dropdown-menu">
                            <li><%= link_to 'Редактирование профиля', edit_user_path(current_user) ,class: 'dropdown-item'%></li>
                            <li><hr class = "dropdown-devider"></li>
                            <li><%= link_to 'Выйти', session_path, class: 'dropdown-item', data: {method: :delete} %></li>
                        </ul> 
                    </li>
                <% else %>
                    <li><%= nav_tab 'Вход',new_session_path, class: 'nav-link px-2', current_page: local_current_page %></li>
                    <li><%= nav_tab 'Регистрация',new_user_path, class: 'nav-link px-2', current_page: local_current_page %></li> <!-- root - префикс страницы (смотреть в rails/routes), куда мы переходим, _path - указание на отностительность ссылки --->
                <% end %>
            </ul>
            </nav>
        </div>
        </header>
<% end %> <!-- Разом записали в main_menu весь этот код -->